# balloon-planner
Scientific ballooning mission planning package

To use the GUI with TIM-like observing constraints, invoke

```
python target_quicklook_gui.py tim_constrains.json
```

To use default constraints (0-90 el, all az), invoke

```
python target_quicklook_gui.py
```